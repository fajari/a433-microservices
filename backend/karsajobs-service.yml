apiVersion: v1
kind: Service
metadata:
  labels:
    app: karsajobs
  name: karsajobs
  namespace: dicoding
spec:
  # Menggunakan tipe NodePort supaya bisa diakses dari luar cluster
  ports:
    - port: 8080
      nodePort: 30001
  selector:
    app: karsajobs
  type: NodePort
